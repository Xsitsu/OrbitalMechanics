using System;
using System.Collections.Generic;
using System.Text;
using System.Numerics;
using OrbitalMechanics.Solver;

namespace OrbitalMechanics
{
    public class TwoBodySolver : ISolver
    {


/*
        public Vector2 GetOrbitalOffset_m(double atTime_sec, Orbit orbit, double centerMass_kg)
        {
            double meanAnomaly = 0;
            double eccentricAnomaly = meanAnomaly * 1;
            return new Vector2(Math.Cos(eccentricAnomaly) * orbit.SemiMajorAxis_m, Math.Sin(eccentricAnomaly));
        }
*/
    }
}
